if (interactive()) {
  library(brapi)
  library(magrittr)

  # Need to connect to a database with genetic data

  # con <- ba_db()$xxxxx

  # gmd <- ba_genome_maps_details(con = con, ...)

  # gmd %>% ba_chart

  # dev.off()
}
